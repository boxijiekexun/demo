#include <stdio.h>
#include <limits.h> // 为了使用 INT_MAX

int main() {
    // int num;
    // int min = INT_MAX; // 初始化为最大值
    // int position = -1; // 最小值的位置
    // int index = 0; // 用于记录当前输入数字的位置（从0开始）

    // // 逐个读取输入的数字
    // while (scanf("%d", &num) == 1) {
    //     // 如果当前数字比已知最小值小
    //     if (num < min) {
    //         min = num; // 更新最小值
    //         position = index; // 更新位置
    //     }
    //     index++; // 前进到下一个数字的位置
    // }

    // // 输出最小值和位置（位置从1开始输出）
    // printf("%d\n", min);
    // printf("%d\n", position + 1); // 位置 + 1 是因为输出要求从 1 开始
    printf("hello\n");
    return 0;
}